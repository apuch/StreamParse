env = Environment()

source = [
    'main.c',
    'debug.c'
]

LIBS = ['m', 'rt', 'fl', 'pthread']
CFLAGS='-Wall -Werror -DLOG_LEVEL_DEF=3 -g -Wno-unused-function -std=gnu99'

env.Library(target = 'StreamParse', source = source, 
    LIBS=LIBS, CPATH='.', CFLAGS=CFLAGS)


